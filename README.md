# DOCX to JSON Lesson Extractor

Python tool that extracts text from a DOCX file (e.g. "TEST FILE DOCX")  
and creates a JSON object with:

- page
- text
- context
- language
- formal
- teach
- level
- steps
- audience

  Example output: see `output.json`


